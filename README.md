Basic Language Model that creates new sentences with given text files.

In order to run, have textfile be in the same folder / directory as java files. Indicate that you are executing with specific text files — example: "java TestLanguage BeeMovieScript.txt Constitution.txt FamilyOfBlood.txt FitnessgramPacerTest.txt SansUndertale.txt WereNoStrangers.txt 50"
Run's well enough in VSCode, may need some tweeking for Eclipse.
